'use client';

export default function Loading() {
  return (
    <div className="bg-brand-light w-full h-[100vh] flex items-center justify-center ">
      <div className="animate-zoom">Loading...</div>
    </div>
  );
}
